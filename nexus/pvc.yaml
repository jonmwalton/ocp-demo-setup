kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  annotations:
  finalizers:
  - kubernetes.io/pvc-protection
  name: nexus-pvc
  namespace: nexus
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi